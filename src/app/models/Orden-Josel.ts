export interface Orden {
  mesa: number;
  producto: string;
  cantidad: number;
  precioUnitario: number;
  entregado: string;
  pagado: string;
}
export interface Ordenado {
  producto: string
  cantidad: number
  precioUnitario: number
}
